services:
  meat-monster:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - meat-data:/app/data
    environment:
      - DATABASE_URL=file:/app/data/dev.db
    restart: always

volumes:
  meat-data:
